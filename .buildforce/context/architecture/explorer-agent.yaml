# Explorer Agent Architecture Context
# Created: 2026-02-10

id: explorer-agent
name: "Buildforce Explorer Agent & Hook"
type: structural
status: production
created: "2026-02-10"
last_updated: "2026-02-19"

summary: |
  The buildforce-explorer agent is a thin wrapper that transparently replaces the built-in
  Explore sub-agent for Claude Code, Cursor, and OpenCode. Each agent has its own hook
  mechanism that intercepts explore/Explore dispatch and rewrites it to buildforce-explorer.
  The explorer then reads and executes the context-search skill (SKILL.md), routing all
  exploration through the curated context repository instead of raw file search.

  Hook implementations:
  - Claude: PreToolUse bash hook (.claude/hooks/setup-explorer-subagent.sh)
  - Cursor: preToolUse bash hook (.cursor/hooks/setup-explorer-subagent.sh)
  - OpenCode: TypeScript plugin (.opencode/plugins/buildforce-explorer-redirect.ts)

  The explorer agent itself contains no search logic. It delegates entirely to the
  context-search skill, keeping the skill as the single source of truth.

responsibilities:
  - Intercept Claude Code's built-in Explore sub-agent dispatch via PreToolUse hook
  - Rewrite the subagent_type from "Explore" to "buildforce-explorer" transparently
  - Read and execute the context-search skill instructions
  - Pass the original Explore prompt through as $ARGUMENTS to the skill

dependencies:
  internal:
    - context-search-skill: "Contains all search logic — the explorer agent just reads and follows SKILL.md"
    - settings-merge-utility: "src/utils/settings-merge.ts merges hook config into agent settings and sets executable permissions"
  external:
    - Claude Code hooks system: "PreToolUse event for intercepting tool calls before execution"
    - Cursor hooks system: "preToolUse event for intercepting tool calls"
    - OpenCode plugin system: "Plugin API for intercepting tool execution"
    - Claude Code Task tool: "The tool being intercepted — all sub-agent dispatches go through Task"

files:
  primary:
    - src/templates/agents/buildforce-explorer.md
    - src/templates/hooks/claude/setup-explorer-subagent.sh
    - src/templates/hooks/cursor/setup-explorer-subagent.sh
    - src/templates/hooks/opencode/setup-explorer-subagent.ts
  secondary:
    - src/utils/settings-merge.ts
    - src/templates/skills/context-search/SKILL.md

interfaces:
  hook:
    - name: "setup-explorer-subagent"
      event: "PreToolUse"
      matcher: "Task"
      trigger: "subagent_type == 'Explore' in tool_input"
      action: "Rewrites subagent_type to 'buildforce-explorer', preserves original prompt"
      config_location: ".claude/settings.local.json → hooks.PreToolUse[]"
      script_location: ".claude/hooks/setup-explorer-subagent.sh"
  agent:
    - name: "buildforce-explorer"
      tools: "Task, Read, Glob, Grep"
      model: "inherit"
      behavior: "Reads .claude/skills/context-search/SKILL.md and follows its instructions"

design_decisions:
  - decision: "Hook intercepts at PreToolUse rather than blocking/denying the call"
    rationale: |
      Using updatedInput to rewrite the subagent_type is transparent — the caller (Claude)
      doesn't know the redirection happened. This is cleaner than blocking the Explore call
      and having the agent retry with a different type. The hook allows the call through
      with modified input, so there's no error handling or retry logic needed.

  - decision: "Explorer agent is intentionally thin — only reads SKILL.md"
    rationale: |
      The context-search skill is the single source of truth for all search logic. If the
      skill evolves (new explorers, different synthesis), the wrapper agent doesn't need to
      change. This also means the user-invocable /context-search and the hook-redirected
      Explore path execute identical logic.

  - decision: "Hook only fires when subagent_type is exactly 'Explore'"
    rationale: |
      The hook checks for the literal string "Explore" — Claude Code's built-in Explore agent.
      It does not intercept other sub-agent types (general-purpose, Plan, etc.) or custom
      buildforce agents. This surgical targeting prevents interference with other Task tool usage.

  - decision: "Hook script uses jq for JSON processing"
    rationale: |
      Claude Code hooks receive JSON on stdin and expect JSON on stdout. jq is the standard
      tool for this. The script reads tool_input.subagent_type, and if it matches "Explore",
      outputs a hookSpecificOutput with updatedInput. If it doesn't match, the script outputs
      nothing (exit 0), which tells Claude Code to proceed normally.

  - decision: "Hook config merged into settings.local.json, not settings.json"
    rationale: |
      settings.local.json is the local-only settings file (not committed to git). Hook
      configuration is project-local infrastructure that shouldn't be in version control
      alongside the shared settings.json. The mergeHooksSettings utility handles this
      idempotently — safe to run multiple times without duplication.

  - decision: "ensureHooksExecutable runs after every mergeHooksSettings call"
    rationale: |
      ZIP extraction and fs.copy don't preserve Unix execute bits. Without chmod, the hook
      script fails with "Permission denied" on first run. Running chmod unconditionally
      after merge is cheap and prevents a confusing failure mode.

  - decision: "Hook script exits 0 even when no rewrite happens"
    rationale: |
      A non-zero exit would be treated as a hook error by Claude Code. When the Task call
      is not an Explore dispatch (e.g., spawning an Extractor), the script must exit cleanly
      with no output, letting the call proceed unmodified.

hook_mechanism: |
  The full interception flow:

  1. Claude dispatches Task with subagent_type: "Explore" (built-in behavior)
  2. Claude Code fires PreToolUse hooks registered for the "Task" matcher
  3. setup-explorer-subagent.sh receives the full tool call as JSON on stdin
  4. Script extracts subagent_type from tool_input via jq
  5. If "Explore": outputs JSON with updatedInput that rewrites subagent_type
     to "buildforce-explorer" and preserves the original prompt
  6. If not "Explore": outputs nothing and exits 0 (no-op)
  7. Claude Code applies updatedInput, replacing the original tool_input
  8. Task now spawns buildforce-explorer instead of built-in Explore
  9. buildforce-explorer reads SKILL.md and dispatches Context Explorers

  The hook JSON output structure:
  ```json
  {
    "hookSpecificOutput": {
      "hookEventName": "PreToolUse",
      "permissionDecision": "allow",
      "updatedInput": {
        "subagent_type": "buildforce-explorer",
        "prompt": "<original prompt>"
      }
    }
  }
  ```

  The settings.local.json hook registration:
  ```json
  {
    "hooks": {
      "PreToolUse": [{
        "matcher": "Task",
        "hooks": [{
          "type": "command",
          "command": ".claude/hooks/setup-explorer-subagent.sh"
        }]
      }]
    }
  }
  ```

distribution: |
  The explorer agent and hook are distributed through the standard Buildforce pipeline:

  BUILD: create-release-packages.sh
  - Agent: generate_agents() processes buildforce-explorer.md (agents: [claude] filtered)
  - Hook: Direct file copy from src/templates/hooks/ to .claude/hooks/ (Claude case only)

  INIT: setup.ts
  - Agent: Extracted from ZIP as part of .claude/agents/ (generic extraction)
  - Hook: Extracted from ZIP as part of .claude/hooks/ (generic extraction)
  - Config: mergeHooksSettings() adds hook entry to settings.local.json
  - Permissions: ensureHooksExecutable() sets chmod on .sh files

  UPGRADE: execution.ts
  - Agent: Replaced as part of .claude/agents/ full replacement (backup + remove + copy)
  - Hook: Replaced as part of .claude/hooks/ full replacement (backup + remove + copy)
  - Config: mergeHooksSettings() re-merges hook entry (idempotent)
  - Permissions: ensureHooksExecutable() re-sets chmod
  - Rollback: Hook directory restored from backup on failure

per_agent_hook_details: |
  CLAUDE CODE (.claude/hooks/setup-explorer-subagent.sh):
    Event: PreToolUse
    Matcher: "Task" (registered in settings.local.json hooks.PreToolUse[])
    Trigger: subagent_type === "Explore" (capital E)
    Input: JSON on stdin with tool_input.subagent_type and tool_input.prompt
    Processing: jq -r extracts subagent_type and prompt from tool_input
    Output (match): JSON with hookSpecificOutput containing:
      - hookEventName: "PreToolUse"
      - permissionDecision: "allow"
      - updatedInput: { subagent_type: "buildforce-explorer", prompt: <original> }
    Output (no match): Empty stdout, exit 0 (no-op, call proceeds unchanged)
    Exit code: Always 0 (non-zero would be treated as hook error)
    Config location: .claude/settings.local.json -> hooks.PreToolUse[]
    Script location: .claude/hooks/setup-explorer-subagent.sh
    Permission: Must be chmod +x (ensureHooksExecutable handles this)

  CURSOR (.cursor/hooks/setup-explorer-subagent.sh):
    Event: preToolUse (lowercase, Cursor convention)
    Matcher: "Task" (registered in hooks.json hooks.preToolUse[])
    Trigger: subagent_type === "explore" (lowercase e - KEY DIFFERENCE from Claude)
    Input: JSON on stdin with tool_input.subagent_type and tool_input.prompt
    Processing: Same jq pattern as Claude
    Output (match): JSON with:
      - decision: "allow" (not "permissionDecision" - KEY DIFFERENCE from Claude)
      - updated_input: { subagent_type: "buildforce-explorer", prompt: <original> }
        (snake_case "updated_input", not camelCase "updatedInput" - KEY DIFFERENCE)
    Output (no match): Empty stdout, exit 0
    Exit code: Always 0
    Config location: .cursor/hooks.json -> hooks.preToolUse[]
    Config format: { version: 1, hooks: { preToolUse: [{ matcher: "Task", command: "..." }] } }
    Script location: .cursor/hooks/setup-explorer-subagent.sh
    Permission: Must be chmod +x

  OPENCODE (.opencode/plugins/buildforce-explorer-redirect.ts):
    Event: tool.execute.before (Plugin API)
    Matcher: input.tool === "task" (lowercase, OpenCode convention)
    Trigger: output.args?.subagent_type === "Explore" (capital E, same as Claude)
    Input: Plugin receives (input, output) parameters with tool name and args
    Processing: Direct property mutation on output.args.subagent_type
    Output: Modifies output.args.subagent_type in-place to "buildforce-explorer"
    No stdout/exit code: Plugin API, not a shell script
    File location: .opencode/plugins/buildforce-explorer-redirect.ts
    Permission: No chmod needed (TypeScript, not bash)
    Note: Plugin is fully REPLACED on each merge (not merged like Claude/Cursor)

  CRITICAL DIFFERENCES SUMMARY:
  ┌──────────────────────┬──────────────┬──────────────┬──────────────────┐
  │ Aspect               │ Claude       │ Cursor       │ OpenCode         │
  ├──────────────────────┼──────────────┼──────────────┼──────────────────┤
  │ Hook type            │ bash script  │ bash script  │ TS plugin        │
  │ Event casing         │ PreToolUse   │ preToolUse   │ execute.before   │
  │ Trigger string       │ "Explore"    │ "explore"    │ "Explore"        │
  │ Output key (rewrite) │ updatedInput │ updated_input│ (in-place)       │
  │ Permission key       │ permission   │ decision     │ N/A              │
  │   Decision           │              │              │                  │
  │ Settings file        │ settings     │ hooks.json   │ plugin .ts file  │
  │                      │ .local.json  │              │                  │
  │ Merge strategy       │ additive     │ additive     │ full replacement │
  │ Needs chmod          │ yes (.sh)    │ yes (.sh)    │ no (.ts)         │
  └──────────────────────┴──────────────┴──────────────┴──────────────────┘

edge_cases: |
  Hook Error Handling:
  - If jq is not installed, Claude/Cursor hooks fail silently (jq commands produce no output)
  - If hook script is not executable, Claude/Cursor report "Permission denied" on first Task call
  - ensureHooksExecutable only runs on non-Windows (process.platform !== "win32")
  - Hook scripts always exit 0 even when no rewrite happens (exit non-zero = hook error)

  Agent Template Filtering:
  - buildforce-explorer.md has agents: [claude, cursor, opencode] - all agents get it
  - The agents: field is stripped from frontmatter during packaging (generate_agents awk)
  - Cursor/OpenCode agents go through normalize-agent-templates.ts for tool name adjustments

  Settings Merge Edge Cases:
  - Claude: If settings.local.json has malformed JSON, it's replaced with empty {}
  - Cursor: If hooks.json doesn't exist, it's created fresh with version: 1
  - OpenCode: Plugin is always fully rewritten (no merge), so no conflict risk
  - Multiple init/upgrade cycles: deduplication via JSON.stringify prevents duplicate hooks

  Distribution Edge Cases:
  - OpenCode plugin is RENAMED during packaging: setup-explorer-subagent.ts → buildforce-explorer-redirect.ts
  - Claude/Cursor hooks keep original filename: setup-explorer-subagent.sh
  - If src/templates/hooks/{agent}/ directory is missing, build_package silently skips hooks

related_context:
  - context-search-skill
  - sub-agent-architecture
  - cli-architecture
  - settings-merge-utility
